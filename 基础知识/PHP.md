# PHP

## 1.立刻执行函数

写作如下格式的php函数为立即执行函数

```
// 写法1：
(
	function(X, x) {
		...
	}
)(参数1, 参数2);

// 写法2：
(
	function(X, x) {
		...
	}(参数1, 参数2)
);

// 写法3：
!function(X, x) {
	... 
}(参数1, 参数2);

+function(X, x) {
	... 
}(参数1, 参数2);
```

